// Generated by CoffeeScript 1.7.1
(function() {
    window.initSliders = function() {
        var factor, left, lineDivWidth, marker, max, mid, min, slider, sliderWidth, sliders,
            value, _i, _len, _results;
        $(window).resize(initSliders);
        sliders = $('.slider');
        sliderWidth = sliders.width();
        lineDivWidth = sliders.find('.marker .line div').width();
        _results = [];
        for (_i = 0, _len = sliders.length; _i < _len; _i++) {
            slider = sliders[_i];
            slider = $(slider);
            min = Number(slider.find('.min').text());
            max = Number(slider.find('.max').text());
            mid = (min + max) / 2;
            _results.push((function() {
                var _j, _len1, _ref, _results1;
                _ref = slider.find('.marker');
                _results1 = [];
                for (_j = 0, _len1 = _ref.length; _j < _len1; _j++) {
                    marker = _ref[_j];
                    marker = $(marker);
                    value = marker.find('.value').text();
                    factor = 0.5 + (value - mid) / (max - min);
                    left = factor * sliderWidth - lineDivWidth;
                    _results1.push(marker.css('left', left));
                }
                return _results1;
            })());
        }
        return _results;
    };

}).call(this);
