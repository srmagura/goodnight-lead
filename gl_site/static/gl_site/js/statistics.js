/**
 * Statistics view. Document on load.
 */
$(function() {
    // Make a jQuery ajax GET request to load organization data.
    // $.get("/statistics/load_organizations", function(data) {

    // $("#id_organization").add('test', 'test');
    // for (var field in data) {
    //     $("#id_organization")[0].options += new Option(1, field);
    // }
    // });
});
