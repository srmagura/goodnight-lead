// Generated by CoffeeScript 1.8.0
(function() {
  var equalizeHeights;

  $(document).ready(function() {
    return equalizeHeights(true);
  });

  equalizeHeights = function(initial) {
    var el, elements, maxHeight, _i, _len;
    if (initial == null) {
      initial = false;
    }
    elements = $('.equal-height');
    maxHeight = 0;
    for (_i = 0, _len = elements.length; _i < _len; _i++) {
      el = elements[_i];
      el = $(el);
      if (el.height() > maxHeight) {
        maxHeight = el.height();
      }
    }
    if (initial || $size === "full") {
      return elements.height(maxHeight);
    } else {
      return elements.height('auto');
    }
  };

  window.sizeChange = equalizeHeights;

}).call(this);
